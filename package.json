{
  "name": "yomitan-lite-frontend",
  "version": "1.0.0",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.js --bundle --outfile=dist/bundle.js && sudo cp index.html styles.css /var/www/yomitan/ && sudo cp -r dist /var/www/yomitan/",
    "watch": "esbuild src/app.js --bundle --outfile=dist/bundle.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "lodash": "^4.17.21",
    "wanakana": "^5.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
